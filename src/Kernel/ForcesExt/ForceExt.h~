#ifndef _FORCE_EXT_
#define _FORCE_EXT_

#include <Vector3.h>

class ForceExt {

	public:
		ForceExt();
		ForceExt(const ForceExt& F);
		~ForceExt();

		//virtual void evaluate(double* pos, double* mass, double* accumForceBuffer, uint nbBodies) = 0;
};

#endif
